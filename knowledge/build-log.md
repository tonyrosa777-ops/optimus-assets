# build-log.md — Optimus Business Solutions
# Cross-Project Knowledge Base
# Location: C:\Projects\Optimus Assets\knowledge\build-log.md
#
# HARD RULES (enforced by CLAUDE.md):
#   - Read this at the start of every build session
#   - Log every resolved error before continuing work
#   - Log every non-obvious pattern or finding at phase completion
#   - Write a retrospective entry at project close

---

## Error Encyclopedia

Known problems with confirmed solutions. Detailed entries in `knowledge/errors/`.

| # | Problem | Project | Date | Entry |
|---|---------|---------|------|-------|
| 1 | Vercel 404 on subdirectory deploy | Enchanted Madison | Mar 2026 | [[errors/vercel-subdirectory-404]] |
| 2 | Nav overflow with >4 links | Enchanted Madison | Mar 2026 | [[errors/nav-overflow-too-many-links]] |
| 3 | Placeholder CTA accepted as phase-complete | Enchanted Madison | Mar 2026 | [[errors/placeholder-cta-accepted-as-complete]] |
| 4 | Generated assets not committed with task | Enchanted Madison | Mar 2026 | [[errors/generated-assets-not-committed]] |
| 5 | Media asset placed in repo root instead of /public | Enchanted Madison | Mar 2026 | [[errors/media-asset-wrong-location]] |
| 6 | /prime loads global template instead of project variables when project-scoped .claude/commands/prime.md doesn't exist | Placed Right Fence | Apr 2026 | [[errors/prime-loads-global-template-instead-of-project]] |
| 7 | Zod v4: `errorMap` renamed to `error` in enum/string params | Enchanted Madison | Mar 2026 | [[errors/zod-v4-errormap-rename]] |
| 7 | Stripe 21: API version changed to `2026-03-25.dahlia`; `shipping_details` moved to `collected_information.shipping_details` | Enchanted Madison | Mar 2026 | [[errors/stripe-21-api-version-dahlia]] |
| 8 | Framer Motion v12: `ease: number[]` (cubic bezier array) rejected in `Variants` — use `"easeOut" as const` or named easing string instead | Sylvia Rich | Apr 2026 | [[errors/framer-motion-v12-ease-type]] |
| 8b | Framer Motion v12: named ease string (`'easeOut'`) inferred as `string` not `Easing` in object literals — TypeScript rejects in Variants; fix: use cubic bezier `[0, 0, 0.2, 1] as const` instead | Where2 Junk | Apr 2026 | (inline fix) |
| 8c | lucide-react: `Facebook` icon was removed from the package — `Export Facebook doesn't exist`; fix: replace with inline SVG path | Where2 Junk | Apr 2026 | (inline fix) |
| 8d | Server Components with `onMouseEnter`/`onMouseOut` event handlers cause prerender crash — `Event handlers cannot be passed to Client Component props`; fix: add `'use client'` as first token. **Exception:** if the component exports `metadata`, it cannot become a Client Component (Next.js prohibits `metadata` in Client Components); use CSS `:hover` in a `<style>` block instead of JS event handlers. See error #27. | Where2 Junk | Apr 2026 | [[errors/server-component-hover-metadata-constraint]] |
| 9 | Turbopack: `"use client"` must be the absolute first token — before imports AND comments. `// comment` then `"use client"` causes build failure | Sylvia Rich | Apr 2026 | [[errors/turbopack-use-client-not-first-token]] |
| 10 | Canvas animation `groundY` breaks after hero flex changes from `items-center` → `items-start` — content pins to top but groundY still scales with H | Placed Right Fence | Apr 2026 | [[errors/canvas-animation-breaks-after-hero-flex-change]] |
| 11 | Mobile viewport height variance (667–932px) causes canvas animation tip to land behind CTA buttons — raw `H * N` groundY not safe without Math.min() cap | Placed Right Fence | Apr 2026 | [[errors/mobile-viewport-height-variance-clips-canvas-animation]] |
| 12 | Sanity Studio SSR crash with Turbopack — `createContext` fires in server context; fix: extract StudioClient as `"use client"` file + `dynamic(ssr:false)` | Placed Right Fence | Apr 2026 | [[errors/sanity-studio-ssr-crash-createcontext-turbopack]] |
| 13 | Vercel deploy blocked after pipeline connected — git committer email didn't match GitHub account; fix: `git config user.email` + empty commit | Danielle Thompson | Apr 2026 | [[errors/vercel-deploy-blocked-git-email-mismatch]] |
| 14 | CTABanner props named `ctaLabel`/`ctaHref` on new page — component interface uses `buttonText`/`buttonHref`; TypeScript build failure on Vercel | Danielle Thompson | Apr 2026 | [[errors/ctabanner-prop-name-mismatch-build-failure]] |
| 15 | `next.config.ts` not supported in Next.js 14 — must use `next.config.mjs` | Danielle Thompson | Apr 2026 | [[errors/next-config-ts-not-supported-nextjs14]] |
| 16 | Rate badge overflow — long policy text in `whitespace-nowrap` pill; fix: use short pointer ("See travel policy"), put detail in notes block | Danielle Thompson | Apr 2026 | [[errors/rate-badge-overflow-whitespace-nowrap]] |
| 17 | Stripe webhook 307 — non-www domain redirects to www; Stripe doesn't follow redirects; webhook silently fails | Andrea Abella Marie | Apr 2026 | [[errors/stripe-webhook-307-www-redirect]] |
| 18 | GHL Private Integration 403 — agency admin token cannot write to location; token exchange endpoint also 401; fix: use GHL Inbound Webhook instead | Andrea Abella Marie | Apr 2026 | [[errors/ghl-private-integration-agency-token-403]] |
| 19 | Contact form empty `onSubmit` — `e.preventDefault()` stub left as placeholder; discovered post-launch when client received no inquiries | Andrea Abella Marie | Apr 2026 | [[errors/contact-form-empty-onsubmit-discovered-post-launch]] |
| 20 | Stripe guest checkout no receipt email — `customer_creation: "always"` required for Stripe to send receipt to guest customers | Andrea Abella Marie | Apr 2026 | [[errors/stripe-guest-checkout-no-receipt-email]] |
| 21 | Printful color-only variant names misparsed as size — 2-part names "Product / Black" assign color to size field; fix: KNOWN_COLORS detection + case-insensitive COLOR_MAP | Andrea Abella Marie | Apr 2026 | [[errors/printful-color-only-variant-misparse]] |
| 22 | Printful CDN image URLs fabricated for products without confirmed scrape URLs — lazy-loaded images appear as SVG placeholders in firecrawl; guessed hash paths 404; use Unsplash until real mockups exist | Cody's Complete Junk Removal | Apr 2026 | [[errors/printful-cdn-image-url-fabricated]] |
| 23 | No global .gitconfig on Windows — fresh Windows install has no ~/.gitconfig; `git config --global user.name` errors; fix: set per-repo identity before first commit | Cody's Complete Junk Removal | Apr 2026 | [[errors/no-global-gitconfig-windows]] |
| 24 | `react-intersection-observer` uses `triggerOnce` not `once` — `once: true` is silently ignored; animations re-trigger on every scroll; fix: `triggerOnce: true` across all animation primitives | Gray Method Training | Mar 2026 | [[errors/react-intersection-observer-triggeronc-not-once]] |
| 25 | Mobile hero text starts mid-screen — `min-h-screen` + `flex items-center` vertically centers content; fine on desktop tall viewport, puts headline dead-center on short mobile screen; fix: `items-start` + `pt-24 md:pt-40` on content div | Sweep Test 1 | Apr 2026 | (inline fix) |
| 26 | JSON `null` vs TypeScript `undefined` in seeded data files — type `string \| undefined` does not accept `null`; fix: omit the field entirely from JSON (absent key = `undefined` in TS) | Where2 Junk | Apr 2026 | [[errors/json-null-vs-typescript-undefined-seeded-data]] |
| 27 | Server Component with `metadata` export cannot use `'use client'` to fix event handler crash — Next.js prohibits `metadata` in Client Components; fix: use CSS `:hover` in `<style>` block instead of JS event handlers | Where2 Junk | Apr 2026 | [[errors/server-component-hover-metadata-constraint]] |
| 28 | Photo placeholder appears in hero instead of 3-layer animation stack — template's split-layout description implied a right-side image slot; fix: hero has NO photo slot; photo belongs in About section; updated CLAUDE.md Hero Architecture Rule and template Section 1 | Helen Grondin | Apr 2026 | (inline fix — CLAUDE.md + template updated) |
| 29 | Hero text invisible against background — wrong color token used for headings; "Where healthcare finally makes sense" unreadable without highlighting; fix: ALL headings use `color: var(--text-primary)` on dark backgrounds; tagline must use `.text-shimmer` class | Helen Grondin | Apr 2026 | (inline fix — CLAUDE.md + template updated) |
| 30 | Second hero CTA links to info session instead of quiz — site.ts `ctaSecondary` pointed to an event link; fix: secondary CTA slot is permanently reserved for `/quiz`; updated CLAUDE.md Hero Architecture Rule and template CTA pair pattern | Helen Grondin | Apr 2026 | (inline fix — CLAUDE.md + template updated) |
| 31 | Testimonials grid orphan rows — 32 testimonials paginated 8 per page in a 3-col grid = 2 full rows + 2 orphans on every page; fix: use 36 testimonials paginated 9 per page (3-col × 3-row = perfect square every page); updated CLAUDE.md and template | Helen Grondin | Apr 2026 | (inline fix — CLAUDE.md + template updated) |
| 32 | Interior pages shipped with static plain headers — only the hero had animations; services, testimonials, booking pages were flat; fix: Page Animation Rule added to CLAUDE.md requiring branded animation on every page | Helen Grondin | Apr 2026 | (inline fix — CLAUDE.md updated) |
| 33 | Behold API integration — 3 compounding bugs: (1) `mediaUrl` is nested under `sizes.medium.mediaUrl` not top-level; (2) Behold UI copies full URL but code prepended base URL again, creating double URL; (3) fallback chain preferred expiring Instagram CDN `mediaUrl` over stable Behold CDN `sizes.*` URLs | Gray Method Training | Apr 2026 | [[errors/behold-api-integration-pitfalls]] |
| 34 | `PhotoPlaceholder` `onError` fires on production even when real image exists — hydration timing issue causes scaffold to swap in placeholder UI; fix: replace `PhotoPlaceholder` with direct `next/image` once real asset is confirmed | Gray Method Training | Apr 2026 | [[errors/photo-placeholder-onerror-fires]] |
| 35 | Canvas particle grid cannot render antialiased text — square pixels at any density produce unreadable blurry text in logos; denser sampling does not help; fix: hybrid approach — use particles for assembly animation only, crossfade to `drawImage()` with `imageSmoothingQuality: "high"` for idle state | Gray Method Training | Apr 2026 | [[errors/canvas-particle-grid-cannot-render-text]] |
| 36 | Canvas effect overlays wash out text on logos — additive blend modes (`lighter`, `shadowBlur`) on rotating/traveling effects push already-bright pixels toward white, destroying text contrast; fix: `ctx.clip("evenodd")` with full-canvas + banner-band rects to carve a hole where no effects draw; also filter sparkle spawn positions at build time | Gray Method Training | Apr 2026 | [[errors/canvas-effect-overlay-washes-out-text]] |
| 37 | Fixed-rem `--text-display: 4.5rem` H1 breaks at mobile width — "More energized." orphaned "More" on its own line at 390px; typecheck and desktop both clean, only visible bug. Fix: `clamp(2.5rem, 8vw, 4.5rem)` — min covers 375px, max preserves desktop; update CSS variable AND matching `@utility` class | Gray Method Training | Apr 2026 | [[errors/fixed-rem-display-font-size-breaks-mobile]] |
| 38 | Desktop "More" dropdown items missing from mobile nav drawer — `MORE_LABELS` Set filters links into desktop-only overflow dropdown but mobile drawer only renders `navigation.links` + hardcoded extras; Shop and FAQ unreachable on mobile. **Orchestrator had the screenshot and missed it** — browser audit needs explicit link-count verification against sitemap, not just open/close/overlay checks. Fix: add every More item to mobile drawer; add link-count gate to Stage 1I checklist. | JCM Graphics | Apr 2026 | [[errors/more-dropdown-items-missing-from-mobile-nav]] |
| 39 | Undefined spacing CSS variables (`--space-lg`, `--space-xl`, `--space-md`, `--space-2xl`) used in 6+ component files but never declared in globals.css — all gaps/padding/margins resolve to 0px; zero build/lint/console warnings; only visible in browser. Fix: add complete `--space-xs` through `--space-3xl` scale to `:root` | JCM Graphics | Apr 2026 | [[errors/undefined-spacing-css-variables-collapse-layout]] |
| 40 | Fixed navbar covers interior page content — `position: fixed` nav overlays page content; interior pages start at y=0 under the opaque navbar; fix: add global `nav-height` offset (padding-top) to `<main>` in layout.tsx during scaffold, not as polish | JCM Graphics | Apr 2026 | [[errors/fixed-navbar-covers-interior-page-content]] |
| 41 | CTA routing inconsistency — "Get a Free Quote" buttons scattered between `/contact` and `/booking` across pages built by parallel agents; fix: enforce primary CTA → `/booking`, secondary → `/quiz` site-wide; define CTA href constants in site.ts; add to agent briefings and pre-launch auditor | JCM Graphics | Apr 2026 | [[errors/cta-routing-inconsistency-contact-vs-booking]] |
| 42 | Missing `text-h1` CSS utility — `text-display`, `text-h2`–`text-h4` defined but not `text-h1`; pages using `text-h1` rendered at default size; Tailwind ignores unknown classes silently; fix: define `text-h1` then standardize all H1s to `hero-shimmer font-display text-display` | Witt's Restoration | Apr 2026 | [[errors/missing-text-h1-css-utility]] |
| 43 | Duplicate CTA sections back-to-back on homepage — BookingPreview + FinalCTA had same intent, different words; created by separate agents; fix: remove duplicate, keep single strong closing CTA | Witt's Restoration | Apr 2026 | [[errors/duplicate-cta-sections-homepage]] |
| 44 | Canvas `offsetWidth/Height` inside fixed/absolute container returns 0 — RisingAsh rendered in tiny corner; fix: use `parentElement.getBoundingClientRect()` and observe parent with ResizeObserver | Witt's Restoration | Apr 2026 | [[errors/canvas-offsetwidth-inside-fixed-container]] |
| 45 | Pricing page includes services Optimus doesn't offer — "Google-ready" and technical feature names leaked from template; fix: remove Google refs, use client-facing names | Witt's Restoration | Apr 2026 | [[errors/pricing-page-includes-services-not-offered]] |
| 46 | Sanity Contributor token cannot publish documents — `create` permission required for non-draft `_id`; fix: Editor role token, OR write to `drafts.<id>` prefix for human publish step | Andrea Abella Marie | Apr 2026 | [[errors/sanity-contributor-token-cannot-publish]] |
| 47 | Sanity asset re-upload fails with 403 "update" required — Sanity dedupes by SHA-1 content hash; second upload treated as update; Contributor lacks `update` on assets; fix: catch 403, lookup existing asset by SHA-1 via GROQ, reuse its `_id` | Andrea Abella Marie | Apr 2026 | [[errors/sanity-asset-reupload-403-update-permission]] |
| 48 | Hero `<Image fill>` background intercepts pointer events — CTAs in hero look correct but clicks do nothing; Playwright literally says `<img> intercepts pointer events`; fix: `pointer-events-none` on decorative image + `relative z-10` on content wrapper. Invisible in dev, only caught by clicking every hero CTA. | Enchanted Madison | Apr 2026 | [[errors/hero-image-fill-intercepts-pointer-events]] |
| 49 | `#anchor` links scroll target behind fixed header — destination section's heading tucked under nav after scroll; fix: `scroll-padding-top` on `<html>` with breakpoint values matching header height + 16px buffer. Global one-rule fix for every anchor site-wide. | Enchanted Madison | Apr 2026 | [[errors/anchor-scroll-hidden-behind-fixed-header]] |
| 50 | Resend emails missing `replyTo` — owner notifications use branded `from` address (e.g. quiz@domain.com) that isn't a real inbox; replies bounce with 554 5.4.0; auto-replies to customers also need `replyTo` pointing to owner's real email or customer replies bounce too. VIP/newsletter welcome emails need CAN-SPAM unsubscribe line + physical address. Fix: every `resend.emails.send()` call must have explicit `replyTo` — owner notifications → lead's email, auto-replies → owner's Gmail. Marketing emails must include opt-out mechanism + physical mailing address. | Enchanted Madison | Apr 2026 | [[errors/resend-missing-replyto-and-can-spam]] |
| 51 | Northwest Registered Agent DNS A record won't propagate to Vercel — root domain A record (`@` → Vercel IP) never resolves despite correct value; CNAME for `www` works immediately. Registrar's DNS servers appear to ignore or override A record changes for root domain. Fix: use Northwest's built-in **Forwarding** feature (General tab → Forwarding section) to 301 the root domain to the canonical `www` domain; let CNAME handle the actual Vercel connection. Only affects non-GoDaddy registrars — GoDaddy A records propagate fine. | Placed Right Fence | Apr 2026 | [[errors/northwest-dns-a-record-wont-propagate]] |
| 52 | Calendly `pageSettings.textColor` is honored only for primary event-title text — secondary elements ("Select a Date & Time" header, month label, day-of-week labels, disabled dates, "Time zone" label, footer links) render in Calendly's internal low-contrast blue-gray and ignore every hex we pass. `backgroundColor` and `primaryColor` both work; `textColor` is effectively broken for dark themes. Fix: stop forcing dark mode — use Calendly's native light theme in a dedicated light-stage section. See [[patterns/calendly-light-stage-section]]. | Collaborative Insights | Apr 2026 | [[errors/calendly-dark-mode-textcolor-incomplete]] |
| 53 | Native date/time pre-picker + Calendly iframe = double-entry kills conversion — Calendly URL params pre-fill `month` and `date` but not `time`, so a user who picks 2pm on our beautiful native calendar must pick 2pm again on the Calendly iframe. Fatal on the most conversion-critical page. Fix: do not build pre-pickers for Calendly; ship the iframe directly + [[patterns/calendly-light-stage-section]] for theming, OR migrate to Cal.com if fully native is required. See [[patterns/conversion-first-friction-audit]] for the preventative workflow rule. | Collaborative Insights | Apr 2026 | [[errors/native-picker-plus-calendly-double-entry]] |
| 54 | Reusable widget invisible on light section — `Card variant="dark"` uses `bg-[var(--bg-card-dark)]` = `rgba(255,255,255,0.04)` (4% white overlay), invisible on cream sections. Combined with internal `--text-primary` (cream) text, the widget renders cream-on-cream when its parent is light. Bug class affects any reusable component dropped into multiple section contexts (LMP's BookingCalendar landed in LO page = dark, /quiz results = light, /booking = light, /pricing modal = overlay). Static page Cards eyeball-paired to their parent at write time aren't affected. Fix: reusable widgets must render a self-sufficient solid surface (`var(--primary)` + border + shadow) instead of relying on the translucent Card variant. See [[patterns/reusable-widget-self-sufficient-surface]]. | LMP Financial | May 2026 | [[errors/reusable-widget-card-variant-mismatch]] |
| 55 | CLAUDE.md absolute rule overridden via agent reference-pattern citation — design-synthesizer cited Claro Advisors single-subject lifestyle photography to justify a two-column hero with photo-on-right, violating CLAUDE.md Hero Architecture Rule's absolute *"No photos in the hero, ever — a photo placeholder in the hero is a build failure."* Orchestrator approved at Stage 1B human checkpoint; animation-specialist built it in Stage 1D; user caught at Stage 1D close. Net cost ~3 hr re-spawn. Lesson: absolute rules ("ever," "always," "never") cannot be agent-overridden via reference-pattern citation regardless of internal consistency of the agent's reasoning. Operational test: "movie-hero" full-bleed cinematic backdrop may be acceptable; "box with a photo in the header" — two-column with photo on right — is NEVER acceptable. Fix: orchestrator runs absolute-rule cross-check at every Stage 1B human checkpoint per [[patterns/claude-md-absolute-rule-cross-check-at-checkpoint]]. | Ead Financial | May 2026 | [[errors/claude-md-absolute-rule-override-via-reference-pattern]] |
| 56 | Cream-on-cream ambient gradient perceptually invisible at viewport scale — Hero v2 ambient backdrop used `--bg-elevated` (`#EEE6DA`) as radial-gradient blob center over `--bg-base` (`#F5EFE6`). The two values differ by ~7-9 RGB points; at 1440x900 viewport scale the perceptual contrast is zero — the gradient was invisible, hero read as "left-aligned text on flat empty page." Root cause: `--bg-elevated` is specced for SECTION-BOUNDARY alternation (where the eye anchors to the boundary line), not for in-section ambient depth. Cream-on-cream gradients require accent injection AND tight geometry — percentage and ellipse size interact non-linearly. Iterative finding (2 Playwright passes, May 2026): `accent 7%/4%` in `85%×70%` ellipse is STILL invisible because the large washed-out blob has no focal point; the eye reads it as flat. Working calibration: `accent 14%` at center, `8%` at 35% stop, in `60%×50%` ellipse at `50% 52%` (centered under H1 optical center) reads as a clear warm pool without "yellow stain." Lesson: a tighter blob at higher accent reads warmer AND less aggressive than a larger blob at lower accent — geometry matters as much as percentage. Falsifies the earlier "6-8% sweet spot" guidance for any ellipse larger than ~60%×50%. See [[patterns/cream-on-cream-ambient-gradient-with-brass-mix]] (update pending). Caught by [[patterns/asymmetric-cost-pre-flight-calibration]] Playwright pass — 15 min check now vs ~3 hr Stage 1I rework after surrounding sections mask the diagnostic signal. | Ead Financial | May 2026 | [[errors/cream-on-cream-gradient-invisible-at-viewport-scale]] |
| 57 | Hero trust microcopy double-rendered stars + credentials — site.ts trustMicrocopy hardcoded `"★★★★★ Trusted by ... · CPA"` AND Hero.tsx ALSO rendered `★★★★★` prefix and `siteConfig.credentials.join(" + ")` suffix. Result: `"★★★★★ ★★★★★ Trusted by ... · CPA · CPA"` visible at both 1440 desktop + 390 mobile. Each agent's output passed its own validation; the bug emerged only when both layers were rendered side-by-side. Root cause: data layer AND component layer both rendered the same presentational decoration. Class-of-bug — will recur on next build's pricing card (`$` prefix), testimonial attribution (em-dash), eyebrow label (`»`), or any component wrapping data with visual marks. Fix: pick a single source of truth per presentational mark (component wins for CSS-renderable, configurable, or fixed-structural marks; data wins for voice-as-content marks); document contract inline next to the data field; pre-launch-auditor runs anti-pattern greps. See [[patterns/single-source-of-truth-for-presentational-decoration]]. | Ead Financial | May 2026 | [[errors/hero-trust-microcopy-double-rendered-decoration]] |

---

## Build Patterns That Work

Specific approaches that produced measurable results.

| # | Pattern | Category | Project | Date |
|---|---------|----------|---------|------|
| 1 | Stripe webhook: always use `collected_information.shipping_details` in v21+; `confirm` must be a query param not body field | Payments | Enchanted Madison | Mar 2026 |
| 2 | Cart drawer in SiteHeader (not layout) — avoids layout refactor while making cart globally accessible | Architecture | Enchanted Madison | Mar 2026 |
| 3 | Stripe API version: always check `node_modules/stripe/types/apiVersion.d.ts` for the correct version string; it changes each major release | Payments | Enchanted Madison | Mar 2026 |
| 4 | fal.ai image generation from design-system.md prompts — eliminates need for client photography before launch | AI Assets | Enchanted Madison | Mar 2026 |
| 5 | Kling AI video for hero background loop — cinematic feel for premium brands without a video shoot | AI Assets | Enchanted Madison | Mar 2026 |
| 6 | Responsive canvas animation with `getLayout(W, H)` breakpoint function — centralizes all layout params, enforces Math.min() cap on mobile groundY | Animation / Architecture | Placed Right Fence | Apr 2026 |
| 7 | Hero concept iteration — budget for 3 full approaches before final; don't start mobile calibration until desktop concept is approved | Workflow / Animation | Placed Right Fence | Apr 2026 |
| 8 | Homepage dark/light section rhythm — plan alternation before building any section; 3 consecutive same-background sections always need a flip | Visual Design / UX | Placed Right Fence | Apr 2026 |
| 9 | 3-tier pricing anchoring — set badge: null on premium tier; only middle tier gets a badge; premium's job is anchoring, not converting | Conversion / Pricing | Placed Right Fence | Apr 2026 |
| 10 | `constants.ts` single source of truth — all client data (phone, email, dates, services, testimonials) in one file; components import, never hardcode | Architecture | Danielle Thompson | Apr 2026 |
| 11 | Google Maps iframe without API key — `maps.google.com/maps?q=City,ST&output=embed` works free; no Cloud account needed | Maps / Integration | Danielle Thompson | Apr 2026 |
| 12 | Next.js native OG image — `opengraph-image.tsx` + `ImageResponse` + `readFileSync` for local asset base64; no external service | SEO / Social | Danielle Thompson | Apr 2026 |
| 13 | Calendly inline embed with brand colors — URL params `background_color`, `text_color`, `primary_color`; `NEXT_PUBLIC_` env var; dynamic script load in useEffect | Scheduling | Danielle Thompson | Apr 2026 |
| 14 | One Resend account per client — free tier = 1 domain; don't consolidate multiple clients into one account; each client owns their account | Email / Architecture | Danielle Thompson | Apr 2026 |
| 15 | Printful variant name parser with KNOWN_COLORS — detect color vs size by name lookup, not position; handles 2-part drinkware and 3-part apparel names; case-insensitive COLOR_MAP | E-commerce / Printful | Andrea Abella Marie | Apr 2026 |
| 16 | GHL CRM integration via Inbound Webhook — create workflow with Inbound Webhook trigger + Create Contact action; POST form data to webhook URL; no API key needed; bypasses all Private Integration auth issues | CRM / Integration | Andrea Abella Marie | Apr 2026 |
| 17 | Stripe webhook canonical URL — register at exact canonical domain (www or non-www); Stripe doesn't follow redirects; NEXT_PUBLIC_SITE_URL must match | Payments / Infrastructure | Andrea Abella Marie | Apr 2026 |
| 18 | Stripe checkout session defaults — always include `customer_creation: "always"`, absolute HTTPS image URLs only, cart in metadata.cart JSON | Payments | Andrea Abella Marie | Apr 2026 |
| 19 | Firecrawl existing site capture as project brief — scrape client's current site into structured .md before build; replaces content questionnaire for clients with existing sites | Workflow / Discovery | Cody's Complete Junk Removal | Apr 2026 |
| 20 | Hero server/client animation split — main hero as server component + separate `HeroEffects` client component for decorative animations; keeps `"use client"` boundary tight, SSRs real content | Architecture / Animation | Cody's Complete Junk Removal | Apr 2026 |
| 21 | Static Printful merch data file — populate shop.ts with real Printful products (correct names, base cost, retail markup) + Unsplash placeholder images; swap images when client generates Printful mockups | E-commerce / Printful | Cody's Complete Junk Removal | Apr 2026 |
| 22 | Dual hero animation system — canvas particles (stars + embers + glimmers, rAF loop) + CSS breathing orbs (`@keyframes orb-breathe`, 3 orbs offset phases) + `text-shimmer` headline; luxury dark-brand hero without video | Animation / Visual Design | Gray Method Training | Mar 2026 |
| 23 | ROI Calculator as dev-only pricing sales tool — interactive calculator + comparison chart env-gated (`NEXT_PUBLIC_SHOW_PRICING_TOOLS`); show to client during sales, remove before launch | Conversion / Pricing | Gray Method Training | Mar 2026 |
| 24 | Quiz multi-step lead capture with program recommendation — 3 steps: multi-select problems → single-select goal → lead form → personalized program CTA; quiz answers appended to contact submission | Conversion / Lead Gen | Gray Method Training | Mar 2026 |
| 25 | Demo booking calendar with hash-based seeded availability — `(year*400 + month*31 + day) % 10` maps dates deterministically; fully interactive without backend; Calendly silently integrates when URL env var is set | Conversion / UX / Demo | Where2 Junk | Apr 2026 |
| 26 | Hover-open + click-navigate nav dropdowns — outer container div gets `onMouseEnter/Leave`; inner trigger is `<Link href="/parent">` not `<button>`; hovering reveals sub-pages, clicking navigates to category page | Navigation / UX | Where2 Junk | Apr 2026 |
| 27 | "More" overflow dropdown for crowded desktop nav — `Set(['Gallery','FAQ','Pricing','Shop'])` filtered from main loop, grouped behind `≡ ∨` click-toggle button; primary conversion links stay always visible | Navigation / UX | Where2 Junk | Apr 2026 |
| 28 | Brand Canvas 5-phase pattern — STREAM (particle bezier flow) → RISE (springOut shape extrude) → COOL (heat palette → brand primary) → ARC (secondary element draw) → IDLE (breathe oscillation); springOut = `1 - 2^(-9t) * cos(t*10π*0.68)`; H1 = siteConfig.tagline + shimmer; text left / canvas right split; `canvas.getContext("2d") as CanvasRenderingContext2D` always cast | Animation / Architecture | Helen Grondin | Apr 2026 |
| 29 | Scored quiz lead funnel — data layer (quiz.ts: QuizType, QUIZ_QUESTIONS×8, QUIZ_RESULTS, scoreQuiz); 3 UI phases (intro→question→results); 400ms pendingAnswer glow + auto-advance; direction-aware AnimatePresence; scoreQuiz on Q8 answer, instant results, no email gate; BookingCalendar inline on results screen; client-only Resend notification; Calendly collects user email during booking | Conversion / Lead Gen | Gray Method Training | Apr 2026 |
| 30 | Behold.so Instagram feed — replaces entire Instagram Graph API + Redis token store + monthly cron system with one env var (NEXT_PUBLIC_BEHOLD_FEED_ID); Behold uses camelCase (mediaUrl not media_url); use plain `<img>` not next/image (CDN domain unpredictable); 9-tile placeholder fallback when ID unset; async server component with hourly ISR; NOT required on every build — optional when client has active Instagram | Social / Integration | Gray Method Training | Apr 2026 |
| 31 | LogoParticles phased reveal — hybrid canvas animation for client logos: 4-phase assembly (converge easeInCubic → hold bright bloom → explode easeOutExpo → idle) with particles drawn from pixel-sampled logo, crossfades to real `drawImage()` for crisp idle state, then overlays rotating ring pulse (arc stroke with shadowBlur) + horizontal shimmer sweep (linear gradient, lighter blend) + additive sparkle bursts on bright pixels; energy effects clipped with `evenodd` rule to exclude the text band [0.34, 0.80]; honors prefers-reduced-motion; resize-safe | Animation / Architecture | Gray Method Training | Apr 2026 |
| 32 | Blog CTA — quiz + book over newsletter capture — replaces end-of-post newsletter signup with two-button card: primary "Find My Program" → /quiz (low friction, feels like a tool), secondary "Schedule a Free Call" → Calendly (external, new tab); no email capture anywhere; headline must be direct ("Stop reading about it. Start doing it."); quiz collects email naturally during booking step; use on every post in the blog template | Conversion / Content | Gray Method Training | Apr 2026 |
| 33 | End-of-build multi-breakpoint live-browser audit — dev server + Playwright at 1440×900 (static + scrolled) + mobile 375/390/428 + mobile nav panel + console check at every viewport; mandatory final gate before ship; catches visible-only bugs (wraps, overflow, hydration, warnings) that typecheck and lint miss; stops dev server with `TaskStop` when done | Workflow / QA | Gray Method Training | Apr 2026 |
| 34 | Clamp-based responsive type scale — every display-tier font size in globals.css declared as `clamp(min, vw-based, max)`; min covers 375px narrowest wrap, max preserves desktop, vw-based preferred scales smoothly; one globals.css edit fixes all uses of the utility; mandatory in Phase 1 project setup, not a retrofit | Typography / Architecture | Gray Method Training | Apr 2026 |
| 35 | Conversion-first hero headline — audit brand-name repetition above the fold, rewrite H1 when brand appears ≥3x (nav + H1 + logo badge); eyebrow qualifies audience, H1 delivers outcome from brand voice doc mission statement, tagline differentiates; swap nav text for image-only if logo badge already contains wordmark; 3-line H1 with shimmer on climax line; copy lives in site.ts `headlineLines` array not JSX | Conversion / Copy | Gray Method Training | Apr 2026 |
| 36 | Chaos→Convergence→Explosion logo reveal — replaces per-letter/per-region assembly (looks broken mid-frame for script fonts); all particles scatter → easeInCubic slam to center energy ball → white-hot hold with pulse → springOut explosion to logo pixels → 160-spark shockwave → drawImage crossfade; every frame intentional; mid-state is glowing orb not partial text | Animation / Architecture | JCM Graphics | Apr 2026 |
| 37 | Page-header ambient radial gradient — CSS-only `radial-gradient(ellipse at 50% 0%, rgba(brand-primary, 0.15), transparent 70%)` on all interior page headers; replaces per-page breathing orbs/particle canvases; consistent, zero-JS, dark-brand only | Visual Design / Animation | JCM Graphics | Apr 2026 |
| 38 | fal.ai: never request readable text in prompts — AI models produce garbled characters ("REJUPED" not "REJECTED"); rewrite prompts to describe scene without text; use FLUX Pro v1.1 for regeneration | AI Assets | Witt's Restoration | Apr 2026 |
| 39 | Consistent page header structure — fragment root (not `<main>`), `relative overflow-hidden` section, `hero-shimmer font-display text-display` H1, heading + subtitle in same section, canvas animation sizes from parent `getBoundingClientRect()` | Architecture / Visual Design | Witt's Restoration | Apr 2026 |
| 40 | Blog index above-fold content — tight spacing (py-12 header, py-6 featured), horizontal featured post layout, 3-col grid, dark→gray→dark color rhythm with category filters on gray band | UX / Content | Witt's Restoration | Apr 2026 |
| 41 | fal.ai reusable blog image generator — parameterized node script `generate-blog-image.mjs <slug> --prompt-file <path>`; queue endpoint for flux-pro/v1.1-ultra; prompts archived in `scripts/prompts/<slug>.txt`; consistent opener/closer across prompts gives visual cohesion without a LoRA | AI Assets / Automation | Andrea Abella Marie | Apr 2026 |
| 42 | Sanity blog post publish script — single-post API publish companion to the bulk migration script; reads `scripts/posts/<slug>.json`, uploads matching image, converts legacy blocks to Portable Text, `createOrReplace` with deterministic `_id`; SHA-1 asset reuse fallback for idempotency; auto-cleans `drafts.` of same ID; Editor token required | CMS / Automation | Andrea Abella Marie | Apr 2026 |
| 43 | `scroll-padding-top` on `<html>` with breakpoint-matched values (mobile 96 / sm 112 / lg 128) — one global CSS rule makes every `#anchor` link on the site scroll with target fully visible below fixed header; replaces per-section `scroll-margin-top` hacks; must be declared during Phase 1 scaffold because bug is invisible during dev | Navigation / CSS / Architecture | Enchanted Madison | Apr 2026 |
| 44 | Resend email `replyTo` checklist — every API route that sends email must set explicit `replyTo` on EVERY `resend.emails.send()` call: owner notification `replyTo` = lead's email (so reply goes to customer); auto-reply `replyTo` = owner's Gmail (so customer reply reaches owner). Any email promising future sends (VIP welcome, newsletter) = marketing under CAN-SPAM: must include "Reply UNSUBSCRIBE to be removed" + physical business address. Audit all routes at launch, not after bounces appear in client's inbox. | Email / Compliance | Enchanted Madison | Apr 2026 |
| 45 | Legal copy as structured data, not JSX — define `LegalBlock` discriminated union (`p` \| `ul` \| `pLink`) + `LegalSection` + `LegalDocument` in `/data/site.ts`; render via shared `LegalRenderer` component that switches on `block.kind`. Keeps privacy/terms/SMS text in one file so carrier-compliance updates (RingCentral/Nextiva/Twilio 10DLC) are a single-file edit. Use `satisfies LegalDocument` for type safety. Do not hardcode legal bodies directly in `page.tsx` — that breaks the site-wide "all copy lives in /data/site.ts" rule and forces JSX edits every time the lawyer sends new text. | Architecture / Compliance | Placed Right Fence | Apr 2026 |
| 46 | No deferred cleanup rule — when a codebase rule violation is discovered inside a file being edited for an unrelated reason, fix the violation in the same commit. Do NOT create "flag for post-launch," "TODO: refactor later," or "tech-debt backlog" items. Defer only when physically blocked (missing client asset, missing API key, pending client decision, locked third-party). Adopted across Optimus builds 2026-04-17 after a launching-phase compliance update almost preserved a CLAUDE.md line-121 violation — client pushed back: "we never want to have to cleanup. If you can do it now, you do it now." Capture this as an Operating Principles section in each client's progress.md so the rule survives future sessions. | Workflow / Quality | Placed Right Fence | Apr 2026 |
| 47 | Opus 4.7 prompt-tuning retrofit — four treatments applied to every agent and skill prompt when a new model ships with more literal interpretation: (1) replace implicit-generalization language ("and anything else relevant") with enumerated lists, (2) harden "write yourself" invention permissions with explicit "IF empty THEN write" clauses + demo-copy marker, (3) add no-fabrication clauses to every pinned count ("real and fewer beats padded and target-met"), (4) strip interim-progress scaffolding now that 4.7 narrates natively. Re-run across the full agent + skill stack whenever a future Claude model ships with similar release-note signals. See [[patterns/opus-4-7-prompt-tuning]] | Workflow | Optimus cross-project | 2026-04-17 |
| 48 | Opus 4.7 API migration checklist (forward-reference for future AI upsells) — breaking changes in the 4.7 Anthropic SDK: `temperature`/`top_p`/`top_k` removed (400 on send), `budget_tokens` removed (use `effort`), adaptive thinking off by default, thinking content hidden unless `display: summarized`. New tokenizer inflates inputs ~1.0–1.45x (code-heavy content at the upper end); bump max_tokens ceilings ~20% and expect cold cache on first switch. Model ID `claude-opus-4-7`. New `xhigh` effort level + task budgets + vision up to 2576px. Forward-reference only — no current Optimus code calls the Anthropic API directly. Consult before the first commit of any n8n Claude node, Retell voice context, AI Chat Assistant upsell, or API-in-Artifacts work. See [[patterns/opus-4-7-api-migration-checklist]] | Workflow | Optimus cross-project | 2026-04-17 |
| 49 | Optimus luxury-modern positioning — every Optimus site ships as a premium, modern, 2026-era build with UI/UX engineered for conversion, regardless of tier or industry. frontend-design.md's aesthetic menu is scoped for Optimus to: luxury/refined, editorial/magazine, modern minimal, organic/natural (premium craft only), or industrial/utilitarian (when industry demands it). NEVER brutalist/raw, retro-futuristic, playful/toy-like, art-deco maximalist, or soft pastel as dominant direction. $1,500 Starter gets same visual floor as $5,500 Premium — cheaper tier means fewer features, not budget-looking design. Copy voice still varies by audience (trade voice for trade clients, aspirational for luxury), but visual presentation is universal premium-modern. See [[patterns/optimus-luxury-modern-positioning]] | Visual Design / Positioning | Optimus cross-project | 2026-04-17 |
| 50 | Claude Design workflow integration — FORWARD-REFERENCE pattern documenting three candidate fits for Anthropic's new Claude Design product (launched 2026-04-17 alongside Opus 4.7, available on Pro/Max/Team/Enterprise): (1) pre-sales pitch deck generated from gap-analyzer output → PDF/Canva attachment making Sales Pattern S9 tangible [LOWEST RISK, pilot first]; (2) pre-scaffold homepage mockup between design-synthesizer and scaffold stage, handoff-to-Claude-Code closes the loop, reduces mid-build revision commits [HIGHEST LEVERAGE]; (3) visual 10-concept hero exploration replacing text concept descriptions in animation-specialist Step 1 [MEDIUM]. Research Preview status, untested quality fit for Optimus Positioning Rule, unknown handoff-bundle code quality — do NOT codify into CLAUDE.md or project-prime.md until piloted. Pilot plan documented in pattern file. Re-evaluate when product exits Research Preview. See [[patterns/claude-design-workflow-integration]] | Workflow / Forward-Reference | Optimus cross-project | 2026-04-17 |
| 51 | Luxury gradient backgrounds — every section on every page is a gradient with subtle motion by default; never flat solid. Motion vocabulary (pick one per section, do not stack): breathing orb (1-2 radial blobs, 12s cycle — default choice), mesh drift (multi-color mesh + animated background-position, 20-30s — premium/hero-adjacent), aurora sweep (conic gradient rotating, 15-25s — dramatic, max 1 per page), grain shimmer (static gradient + noise overlay, 8s — text-heavy-adjacent). Static-gradient exceptions (motion off, gradient still on): long-form text (blog bodies, legal, FAQ), pricing comparison tables, form-dense sections. Performance budget: max 3 active motion layers visible simultaneously; CSS-only (never JS-driven); GPU-cheap properties only (transform/opacity/background-position — no filter/blur on animated layers); ≥50 FPS at 390px mobile. Accessibility: prefers-reduced-motion degrades to STATIC gradient (never flat); motion ≤0.3Hz. Reference brands: Stripe (mesh), Vercel (aurora), Linear (orbs), Raycast/Superhuman (grain). Full CSS recipes + per-page motion budgets in [[patterns/luxury-gradient-backgrounds]] | Visual Design / Motion | Optimus cross-project | 2026-04-17 |
| 52 | Calendly light-stage section — on dark-brand Optimus builds, embed Calendly in its own ivory-bg (`#F5F0EB`) section instead of forcing dark-mode pageSettings (which can't override secondary-element text color). Session selector cards go light (white bg, `#1A1A1A` titles, `#4A4A4A` body, gold active-border + soft shadow). Widget wrapper is pure white with soft elevation shadow. `pageSettings` carries only `primaryColor: "C5A55A"` + `hideGdprBanner` — drop `backgroundColor` and `textColor`. Hero above stays dark; transition becomes intentional "step into the light" for the booking moment. Default choice for any future scheduler iframe with similar theming limits. | Scheduling / Visual Design / Conversion | Collaborative Insights | Apr 2026 |
| 53 | Feature-flag tier-gated features (not deleted) — when client signs below Premium, hide the tier-exclusive feature behind `NEXT_PUBLIC_*_ENABLED` instead of deleting. Shop example: `src/lib/feature-flags.ts` exports `isShopEnabled`, nav filters out `/shop`, cart icon `{isShopEnabled && <CartButton/>}`, `/shop` returns `notFound()`. All Cart/Drawer/Printful/Stripe/seeded-products code stays in repo. CartProvider stays mounted. To reveal on upgrade: set env var in Vercel + redeploy, zero code changes. Default to `=== "true"` (missing env → off). Cheaper than re-scaffolding if client upgrades, cleaner than "delete and pray we kept a branch." See [[patterns/feature-flag-tier-gated-features]] | Architecture / Sales / Post-Sale | Collaborative Insights | Apr 2026 |
| 54 | Conversion-first friction audit (booking / signup / checkout / quiz) — MANDATORY pre-build step for any pre-picker, wizard, or multi-step UI on a conversion-critical page. Map the full journey including third-party iframe fields; flag any field the user enters twice 🚩; name the third-party limitation behind each 🚩; if any 🚩 exists AND the tool can't be replaced, the design is blocked — polish within the existing flow instead. Say the tradeoff out loud in the plan ("building X means picking twice because Y") so the user can veto with facts, not discover friction after rebuild. Conversion-critical pages prioritize FEWER steps over visual polish every time. See [[patterns/conversion-first-friction-audit]] | Workflow / Quality / Conversion | Collaborative Insights | Apr 2026 |
| 55 | Reusable-widget self-sufficient solid surface — any component imported by 2+ consumer files where parent section tones vary (dark/light/modal) MUST render its own solid background (`var(--primary)` + `border-[var(--border-dark)]` + `shadow-lg` for dark widgets; `var(--bg-card)` + `border-[var(--border-light)]` + `shadow-lg` for light widgets) instead of using the translucent `Card variant="dark"`/`"light"` which assumes a fixed parent tone. Static page Cards eyeball-paired to their adjacent section don't need this pattern; reusable widgets do. Pre-launch auditor: list every component imported by 2+ files, map parent sections per consumer, BLOCK if any consumer puts it in a mismatched-tone parent. See [[patterns/reusable-widget-self-sufficient-surface]] for the decision tree, code template, and pitfalls. | Visual Design / Component Architecture | LMP Financial | May 2026 |
| 56 | Huashu-extracted critique rubric + anti-AI-slop checks — narrow methodology adoption from alchaincyf/huashu-design (skill itself NOT installed, license blocks commercial use). Two changes: (a) animation-specialist Step 2 critic expanded from 4 criteria (Niche / Visual impact / Feasibility / Uniqueness) to 6 by adding Visual hierarchy (does animation lead eye to H1+CTA or compete) + Functionality (every element earns its place vs. parasitic decoration) — winner needs ≥22 of 30 AND auto-reject if Hierarchy or Functionality < 3; (b) pre-launch-auditor new Section 3B with two FAIL/WARN gates: no figurative SVG illustrations of people/scenes (FAIL — belongs in photography per Image Generation Rule), no left-accent-border on rounded cards (WARN if >2 components — flagged AI-signature visual). License posture: methodology adoption with attribution is not a license violation, but installing the skill (or its prompts/components) for client work would be. Future trigger to revisit full skill: if Optimus starts shipping pitch decks, motion-design MP4s, or iOS/Android prototype mockups for Tier-4 client demos. See [[patterns/huashu-extracted-critique-rubric]] | Workflow / Visual Design / Quality | Optimus cross-project | 2026-05-02 |
| 57 | Tier-carve-out scope strip — when client buys Pro + a single feature carved out of Premium (here: NSFE bought Pro $3,000 + tool-suite $500), strip the rest of Premium scope from the codebase before launch instead of leaving it dormant. Carve-out playbook: (1) `siteConfig.pricing` Premium tier definition is the source of truth for what's in Premium — any item NOT bought gets cut; (2) delete the seed/scaffold for each unsold Premium item (e.g. `/src/sanity/`, `quiz-premium.ts`); (3) remove Premium-only deps from package.json + `npm install` to regenerate lockfile; (4) `/pricing` always deletes anyway per Always-Built Features Rule; (5) update `website/upsells-for-premium.md` so the next upsell conversation distinguishes the **carved-out feature itself** (paid for) from **layers that sit on top of it** (still sellable — e.g., calculators paid, *PDF lead-gate over calculators* still upsell); (6) verify with `grep -rn "isInternal|/pricing|@sanity|next-sanity|quiz-premium|PricingTier"` returning zero. Distinguish from Pattern #53 (feature-flag tier-gated): use #53 when the unsold tier item might convert later and you want zero-code-change reveal; use this strip when the unsold item is unlikely to be re-sold soon and lockfile/dep weight is worth removing. | Sales / Architecture / Post-Sale | NorthStar Financial Education | 2026-05-07 |
| 58 | CLAUDE.md absolute-rule cross-check at design-synthesizer human checkpoint — orchestrator-level verification step at every Stage 1B human checkpoint. Before approving design-system.md, cross-check every section that touches a CLAUDE.md absolute rule (Hero Architecture Rule, Always-Built Features Rule, Conversion Flow Rule) against that rule's verbatim language. Reject any deviation regardless of how well the agent reasoned about reference patterns to justify it. Absolute rules use language like "ever," "always," "never," "non-negotiable" — search CLAUDE.md for these tokens to enumerate. Reference patterns inform aesthetic + component choices; they cannot override absolute rules. Operational test for hero-photo questions specifically: "movie-hero" full-bleed cinematic backdrop = OK; two-column with photo on right = NEVER OK regardless of which reference site is cited. See [[patterns/claude-md-absolute-rule-cross-check-at-checkpoint]] for the 6-question audit procedure. | Workflow / Quality Gate | Ead Financial | May 2026 |
| 59 | LIGHT-MODE-DOMINANT cream theme — research-justified deviation from Optimus default dark-theme template for financial-services / editorial-register builds. Cream `#F5EFE6` background dominant + deep ink navy `#0F1B2D` text + single muted brass accent `#B07D2A`. Applies when ALL FOUR conditions met: (1) competitor scan documents navy + white + gray saturation in market-intelligence.md §3/§8, (2) audience makes high-anxiety decisions (threat response per Schmader & Beilock 2012), (3) editorial/boutique register fits the audience, (4) differentiation thesis depends on visual escape from competitor zone. Specific tokens including 9-token core + 5-token inverse/dark-section variant. Compatible with Pattern #51 (luxury gradient backgrounds), Pattern #55 (reusable-widget self-sufficient surface), Pattern #60 (Section 12 Psychological Foundations). NEVER acceptable: navy-dominant hero, second accent, flat solid backgrounds. See [[patterns/light-mode-dominant-cream-theme]]. | Visual Design / Theme Architecture | Ead Financial | May 2026 |
| 60 | Cream-on-cream ambient gradient hero backdrop with brass-tinted center — single CSS-only radial-gradient layer behind hero text on cream-dominant builds. Recipe: ellipse 85% 70% at 50% 42%, `color-mix(in oklab, var(--accent) 7%, var(--bg-base))` at center, falling off through 4% mix to base cream at 78%. Slow `transform: translate3d() + opacity` drift on 20s cycle. `prefers-reduced-motion: reduce` → static gradient (NEVER flat per CLAUDE.md anti-pattern #14). Replaces both particle-system canvas heroes (rejected for editorial register per design-system.md §10 anti-pattern #7) and the failed naive cream-on-cream gradient using `--bg-elevated` as blob center (Error #56). Inner column requires `mx-auto` centering — without it the right side reads as empty rail and the centered halo sits behind whitespace. See [[patterns/cream-on-cream-ambient-gradient-with-brass-mix]]. | Visual Design / Animation / Motion | Ead Financial | May 2026 |
| 61 | Section 12 Psychological Foundations — design-system.md addition — 12th section appended to design-system.md (after the canonical 11) that maps each design decision to its behavioral mechanism citation. Converts a design system from "competitively-backed" (decisions cite Claro/competitors) to "psychology-backed" (decisions cite Kahneman & Tversky, Ariely, Thaler, Brehm, Cialdini, Festinger, Sweller, Reber/Schwarz/Winkielman, Crowley & Hoyer, Mehta & Zhu, et al.). 8 subsections: audience emotional state → cognitive fluency → 14-row mechanisms map → commitment-escalation strategy → anchoring strategy → loss-aversion framing → pre-launch-auditor 10-binary-check checklist → defensibility script under client pushback. BINDING for Stage 1D content-writer (diagnostic quiz framing, anchoring on pricing copy, loss-frame openers on niche landings) AND for Stage 1H pre-launch-auditor (10 binary mechanism checks). 600-1100 word length budget. Mandatory on financial/legal/health/compliance builds; high-leverage on every Optimus build. design-synthesizer.md should require Section 12 in its 12-section output spec going forward. See [[patterns/section-12-psychological-foundations-design-system]]. | Design Documentation / Defensibility / Content-Writer Briefing | Ead Financial | May 2026 |
| 62 | NichePage `type` discriminator for shared `[slug]` dynamic route — `type: "vertical" | "funnel"` discriminator field on a `NichePage` data interface lets a single Next.js dynamic `[slug]` route emit different JSON-LD schema bundles per page type while sharing one route file, one section template, one rendering surface. Vertical = FAQPage + Service + Person + LocalBusiness (SEO niche pages). Funnel = HowTo + LocalBusiness (behavioral conversion-funnel pages with different schema needs). Discriminator on the data interface (not on the route file) keeps route generator pure-functional and lets future niches add zero code. TypeScript `never` exhaustiveness check in default branch fails build if a future type literal isn't handled. Each schema bundle reflects the page's AEO target query class — mismatched schema kills the AEO citation case. Section content shape stays shared (lossFrame + mechanism + FAQs + CTA structure works for both types); schema interpretation is what diverges. See [[patterns/nichepage-type-discriminator-shared-slug-route]]. | Architecture / SEO Schema / Data Modeling | Ead Financial | May 2026 |
| 63 | Asymmetric-cost pre-flight calibration before next build stage — workflow rule for deciding whether to verify a recently-rebuilt component NOW (before downstream work starts) or LATER (during canonical end-of-build audit). When the cost of catching a regression now is asymmetric to the cost of catching it later (e.g., 15 min Playwright check now vs. 3 hr rework after Stage 1E builds dependent content on top), always run the cheap check now. Stage 1I multi-breakpoint browser audit is the wrong place to catch hero-composition issues because surrounding page content masks the diagnostic signal — diagnostic loses signal once you add context. Decision rule: if later-cost / now-cost ≥ 5, verify now. Verify in isolation, not against eventual full page. Apply fixes inline (orchestrator-scoped, not agent re-spawn) for 1-3 line calibration regressions. Caught Ead's cream-gradient invisibility error (would have been ~3 hr Stage 1I rework without this pre-flight). Pattern complements (does NOT replace) Pattern #33 end-of-build multi-breakpoint browser audit. See [[patterns/asymmetric-cost-pre-flight-calibration]]. | Workflow / Quality / Cost-Aware Verification | Ead Financial | May 2026 |
| 64 | Single source of truth for presentational decoration — data layer OR component layer, never both — binding rule for every component that wraps data-layer strings with presentational marks (stars `★`, dollar prefixes `$`, percent suffixes `%`, em-dash attributions `— Sarah`, bullet markers `·`, chevron eyebrows `»`, brass dot dividers, parentheses around credentials). Either the data string contains the decoration OR the component renders it, never both. Default rule when in doubt: COMPONENT layer (it has CSS, conditional logic, locale formatting; data layer is content-only). Voice-as-content marks (em dashes embedded inside a sentence) belong in data; structural decoration marks (em-dash attribution separator) belong in component. Contract documented inline next to data field via two-line comment citing the component that owns the decoration. Pre-launch-auditor runs anti-pattern greps to catch duplicates: `grep -nE '"\\s*★'`, `grep -nE '"\\s*\\$\\s*[0-9]'`, `grep -nE '"\\s*—\\s+[A-Z]'` etc. Triggered by Error #57 hero trust microcopy double-render; applies to every Optimus build's pricing card / stats bar / testimonial attribution / eyebrow / breadcrumb / FAQ category. See [[patterns/single-source-of-truth-for-presentational-decoration]]. | Architecture / Component-Data Contract / Quality Gate | Ead Financial | May 2026 |

---

## Project Retrospectives
| Project | Type | Tier | Retro date | Retrospective |
|---|---|---|---|---|
| Ead Financial | CPA practice (tax + outsourced accounting + taxpayer representation) | Premium ($5,500) | 2026-05-09 (early-retro: written between Stage 1D close and Stage 1E spawn) | [[retrospectives/ead-financial]] |

| Project | Type | Completed | Key Learning |
|---------|------|-----------|--------------|
| Enchanted Madison | Luxury glamping / romantic experience | Mar 2026 (Session 14 addendum Apr 2026) | Original: 10 workflow gaps closed. fal.ai + Kling introduced as standard AI asset tools. **Session 14 (Apr 15): responsive/CTA polish pass — 2 new errors (#48 hero image intercepts clicks, #49 anchor behind fixed header), 1 new pattern (#43 scroll-padding-top), 3 toolkit gaps flagged.** See [[retrospectives/enchanted-madison]] |
| Placed Right Fence | Fence installation/repair contractor | Apr 2026 | Canvas forge animation locked after 9 iterations. 3 new errors, 4 new patterns. First patterns/ directory created. See [[retrospectives/placed-right-fence]] |
| Danielle Thompson | Notary Public + Justice of the Peace | Apr 2026 | 4 new errors, 5 new patterns. Bio fabrication caused rework. Git identity + rate badge overflow + CTABanner props key lessons. Calendly/Resend/OG image patterns established. See [[retrospectives/danielle-thompson]] |
| Andrea Abella Marie | Trauma-informed coach + energy healing | Apr 2026 (multiple additions) | Initial build: 5 errors + 4 patterns (Printful+Stripe shop, GHL webhook, color variant parser). Sanity migration Apr 11: embedded Studio + idempotent migration script. Publishing automation Apr 14: 2 new errors (#46-47 Sanity Contributor token limits) + 2 new patterns (fal.ai reusable generator + Sanity single-post publish script). Seven posts added total, three via automated pipeline. See [[retrospectives/andrea-abella-marie]] |
| Cody's Complete Junk Removal | Junk removal contractor — Dracut, MA / Greater Lowell | Apr 2026 | 2 new errors, 3 new patterns. Firecrawl site capture replaces content questionnaire. Hero server/client animation split introduced. Static Printful merch shop pattern established. No CLAUDE.md/progress.md created — flagged as workflow gap. See [[retrospectives/codys-complete-junk-removal]] |
| Gray Method Training | Women's personal training / online fitness coaching | Apr 2026 | 4 sessions: initial (28 commits) + follow-up (8) + polish (7) + conversion pass + audit (2). Total: 6 new errors, 8 new patterns. Session 4 introduced the mandatory end-of-build multi-breakpoint browser audit, caught a fixed-rem H1 wrap bug, and established clamp() type scale + conversion-first hero H1 patterns. See [[retrospectives/gray-method-training]] |
| Sylvia Rich | Honorary Consul of Hungary — New England (5 states) | Apr 2026 (in progress) | Pro package sold. Bilingual (EN/HU formal 3rd person) closed the deal. Domain + Resend set up during meeting. 6 new sales patterns validated (S8–S13). See [[retrospectives/sylvia-rich]] |
| Where2 Junk | Junk removal — Manchester, NH | Apr 2026 (demo-ready, pre-launch) | Full Pro+ build: shop, booking calendar, quiz, blog, 8 service areas, SEO. 2 new errors, 3 new patterns. Demo booking calendar with seeded availability introduced. Nav overflow resolved with More dropdown. Server-component hover CSS fix discovered. See [[retrospectives/where2junk]] |
| JCM Graphics | Large-format printing, vinyl graphics, signage — Salem, NH | Apr 2026 (demo build, Phases 0-9 complete) | 6+ sessions. Hero animation iterated 9+ times → chaos→convergence→explosion (Pattern #36). 4 new errors (#38-41): mobile nav missing More items, spacing CSS vars, fixed-nav overlap, CTA routing inconsistency. 2 new patterns (#36-37): logo reveal + page-header radial gradient. 4 toolkit recommendations pending. See [[retrospectives/jcm-graphics]] |
| Witt's Restoration | 24/7 towing, auto body, restoration, mobile mechanic — Groveton, NH | Apr 2026 (demo-ready, pre-launch) | 3 sessions. 11 post-sweep fixes applied. 4 new errors (#42-45): missing CSS utility, duplicate CTAs, canvas sizing, pricing content. 3 new patterns (#38-40): fal.ai text avoidance, page header consistency, blog above-fold. RisingAsh ambient animation component created. See [[retrospectives/witts-restoration]] |
| Collaborative Insights | Spiritual consulting + ascension guidance — Texas (Zoom) | Apr 2026 (sold Pro, post-sale polish pass) | 3 sessions. Booking widget iterated 6× before landing the light-stage fix. 2 new errors (#52-53): Calendly dark-mode textColor incomplete + native-picker-plus-Calendly double entry. 3 new patterns (#52-54): Calendly light-stage section, feature-flag tier-gated features (shop scaffolding behind env flag), conversion-first friction audit. Post-sale lifecycle validated: pricing page archived to md, shop hidden behind flag (not deleted), headshot integrated once client asset arrived. Anthony's "main point of the site is to convert" feedback codified as cross-project Pattern #54. See [[retrospectives/collaborative-insights]] |

---

## Sales Patterns

Validated sales process knowledge from real client meetings. Full entries in `knowledge/sales/`.

| # | Pattern | Validated In | Date |
|---|---------|-------------|------|
| S1 | Demo sequence: hero → 1-click flow → gallery → quiz → shop → ROI calc → pricing → close. ROI calc always immediately before pricing. | Placed Right Fence | Apr 2026 |
| S2 | Send invoice during the meeting. Do not wait to follow up. | Placed Right Fence | Apr 2026 |
| S3 | Tax write-off + credit card close: converts price from a cost to a tax event. Works for any business owner. | Placed Right Fence | Apr 2026 |
| S4 | Demo site as pre-payment hook: access unlocks on invoice paid. | Placed Right Fence | Apr 2026 |
| S5 | 15-second response time stat: "if you don't respond within 15 seconds, odds of getting their business drop 50%" — use to close automation upsell. | Placed Right Fence | Apr 2026 |
| S6 | Embed all tools directly in the website (calculators, booking, widgets) — never link out. Frame as SEO traffic retention to client. | Placed Right Fence | Apr 2026 |
| S7 | Three-phase upsell sequence: website → marketing/ads → automation. Only introduce next phase after current is sold and launched. | Placed Right Fence | Apr 2026 |
| S8 | Complete in-meeting onboarding (domain + Resend + DNS access) immediately after invoice paid — removes post-sale friction, saves ~1 week. | Sylvia Rich | Apr 2026 |
| S9 | Gap analysis as demo opener — present 5–6 specific competitor failures before showing the site. Positions as category expert before product reveal. | Sylvia Rich | Apr 2026 |
| S10 | Let client play the quiz during demo — they experience their own customer's journey. More effective than narrating it. | Sylvia Rich | Apr 2026 |
| S11 | AEO (AI Engine Optimization) as urgency trigger — ask if they've searched themselves in ChatGPT. If wrong person comes up, getting live fast becomes a race. | Sylvia Rich | Apr 2026 |
| S12 | Blog = email filter framing — find the client's most common time-wasting inquiry, show an article that answers it. "Your site should answer every question so they don't bother you." | Sylvia Rich | Apr 2026 |
| S13 | Save mobile demo for last — "I didn't even show you the best part." Strong closer, especially when competitors' mobile is broken. | Sylvia Rich | Apr 2026 |

See full detail: [[sales/sales-patterns]] and [[sales/sales-language]]

---

## Workflow Improvements Implemented

Changes made to the Optimus toolkit as a result of project learnings.

| Date | Change | Trigger |
|------|--------|---------|
| Mar 2026 | Renamed `design-contract.md` → `design-system.md` | Naming drift across toolkit files caused aliasing hacks |
| Mar 2026 | Added `intake-prompt.md` and `market-research-prompt.md` as global slash commands | Workflow referenced prompts that didn't exist as commands |
| Mar 2026 | Replaced `optimus-pre-build-workflow.md` with `end-to-end-workflow.md` | Manual checklist replaced with fully autonomous Claude command |
| Mar 2026 | Added `build-log.md` knowledge base + Obsidian vault | Learnings from each build were lost when next project started |
| Mar 2026 | Changed progress.md update rule from end-of-session to after-every-subtask | Context exhausted mid-build on Enchanted Madison leaving progress.md undocumented |
| Mar 2026 | 10 gaps from Enchanted Madison closed — nav rule, CTA placeholder rule, asset commit rule, sections matrix BLOCKER, forms intake field, AI asset generation docs (fal.ai + Kling) | Enchanted Madison build debrief gap analysis |
| Apr 2026 | Added Plan Mode Rule + Subagent Delegation Rule + Skill Creation Rule to CLAUDE.md — plan before building, delegate tasks to subagents, create skills from new patterns | Workflow consolidation after Sylvia Rich + PRF sales meetings |
| Apr 2026 | Added Calendly + Google Maps + Homepage Teaser Rule to website-build-template.md — standard booking widget, service area map, every page gets a homepage teaser | Workflow consolidation |
| Apr 2026 | Created `knowledge/onboarding/client-launch-checklist.md` — exact step-by-step for Resend domain setup, DNS→Vercel configuration, env vars, Stripe webhook, pre-launch checklist | Workflow consolidation — steps previously tribal knowledge |
| Apr 2026 | Em dash rule added to CLAUDE.md Content Standards — never use em dash (—) in testimonials; humans don't type them, it's an AI/copywriter tell | Observed in copy review |
| Apr 2026 | Hero mobile padding rule added to animation-specialist.md and project-prime.md — `items-center` on hero section causes mid-screen text on mobile; must use `items-start` + `pt-24 md:pt-40` | Sweep Test 1 |
| Apr 2026 | Hero Architecture Rule hardened — added explicit "no photo in hero ever" + "photo belongs in About section only"; template Section 1 split-layout removed photo slot | Helen Grondin build sweep |
| Apr 2026 | Tagline shimmer made mandatory in CLAUDE.md and template — `text-shimmer` CSS class on hero tagline every build; hero text contrast check added as pre-phase-complete gate | Helen Grondin build sweep |
| Apr 2026 | Second hero CTA locked to quiz — secondary button always `/quiz`, never info sessions or external links; updated CLAUDE.md Hero Architecture Rule | Helen Grondin build sweep |
| Apr 2026 | Testimonials count changed 32→36, pagination changed 8→9 per page — 8 per page in 3-col grid creates orphan rows; 9 fills exactly 3×3 on every page; updated CLAUDE.md and template | Helen Grondin build sweep |
| Apr 2026 | Page Animation Rule added to CLAUDE.md — every interior page must ship with a brand-appropriate animation in its header; static plain pages are a build failure | Helen Grondin build sweep |
| Apr 2026 | **Multi-breakpoint live-browser audit baked into workflow as mandatory pre-ship gate** — dev server + Playwright at 1440 + mobile 375/390/428 + mobile nav open + console check at every viewport. Wired into: CLAUDE.md Visual QA Rule, build-checklist.md Phase 1 step 14, website-build-template.md Checklist: Before Launch → Visual QA, and pre-launch-auditor.md Section 11 handoff. Every prior build shipped without this. See [[patterns/end-of-build-multi-breakpoint-browser-audit]] | Gray Method Training Session 4 |
| Apr 2026 | **Clamp responsive type scale mandatory in Phase 1 globals.css setup** — all display-tier font sizes (>2rem) declared as `clamp(min, vw, max)` in both CSS variable and `@utility`. No more fixed-rem display sizes. See [[patterns/clamp-responsive-type-scale]] | Gray Method Training Session 4 |
| Apr 2026 | **Sanity role selection rule** — when issuing API tokens for CMS-backed sites, default to **Contributor** for unreviewed content pipelines (agent-written, client-submitted, UGC) and write to `drafts.<id>`; use **Editor** only when content has passed a human gate before the script runs. Document the trust model in the script header. See [[errors/sanity-contributor-token-cannot-publish]] | Andrea Abella Marie Apr 14 |
| Apr 2026 | **CMS publish automation scaffolded in Phase 1 for any CMS-backed client publishing ≥1 post/month** — generate-blog-image.mjs + publish-blog-post.mjs + `scripts/posts/` + `scripts/prompts/` directory layout. Retrofitting this is easy but scoping it upfront is cheaper. See [[patterns/sanity-blog-post-publish-script]] + [[patterns/fal-ai-reusable-blog-image-generator]] | Andrea Abella Marie Apr 14 |
| Apr 2026 | **Conversion-first hero H1 pattern** — pre-launch audit counts brand-name instances above the fold; rewrite H1 to outcome-led mission statement if ≥3. Eyebrow qualifies audience, H1 delivers value, tagline differentiates. See [[patterns/conversion-first-hero-headline]] | Gray Method Training Session 4 |
| 2026-04-17 | Full prompt stack tuned for Opus 4.7 (literal interp, tone shift, tokenizer inflation, per-agent effort, /ultrareview gate added as Phase 1J, API-migration-checklist created for future upsells) | Claude upgraded Opus 4.6 → 4.7 on 2026-04-17; prompts needed hardening against literal interpretation and tokenizer cost |
| 2026-04-17 | **Optimus Positioning Rule codified** — added to CLAUDE.md as top-level rule (after Core Law), enforced in design-synthesizer.md Task opening, referenced in content-writer.md Voice Anchor scope note. Pattern #49 created as reference. Previously implicit (Anthony steered every design-synthesizer output manually); now explicit so Opus 4.7's literal interpretation picks luxury-modern-2026-conversion direction by default without manual correction | Stress-test Wave 2 info-item: "universal emoji rule conflicts with luxury positioning" — surfaced that positioning had never been codified; user confirmed "all Optimus sites are luxury, modern, 2026 build for conversion" |
| 2026-04-17 | **Luxury gradient backgrounds rule added** — CLAUDE.md Homepage Section Architecture Rule → Dark/light alternation subsection expanded into Background depth & motion subsection. No flat solid backgrounds ever, on any page, any tone. Every section = gradient + subtle motion by default. 4 motion vocabulary options (breathing orb / mesh drift / aurora sweep / grain shimmer). Static-gradient exceptions for long-form text, pricing tables, form-dense. Performance budget: max 3 active motion layers, CSS-only, GPU-cheap properties. Accessibility: prefers-reduced-motion degrades to static gradient never flat. Visual QA Rule exit criteria extended with 3 new checks (flat-background scan, motion layer count, reduce-motion graceful degrade). Pattern #51 with CSS recipes. | Anthony flagged: "we're going for luxury builds — we should never have just a plain color background, always use gradients with subtle motion" — codified so Opus 4.7 agents don't ship flat sections on light backgrounds (current rule only required gradients on dark) |
